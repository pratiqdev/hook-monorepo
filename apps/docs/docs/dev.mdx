---
hide_title: true
---
import {version} from 'react'
import StatusBadge from '@site/src/components/StatusBadge'
import ClientBadge from '@site/src/components/ClientBadge'
import Button from '@site/src/components/Button'
import { Card, HeadlessTable, MainText, IntroList, HooksByTags, HookWheel, RelatedTable } from '/src/components/'


<div style={{ display: 'flex', alignItems:'center', marginBottom: '1rem' }}>
    <h1 style={{ margin: 0, marginRight: '1rem' }}>Development Status</h1>
    {/* <div className='badge'>beta</div> */}
</div>

The hooks npm package `@pratiq/hooks`, the docusaurus app `@pratiq/docs` and common utils `@pratiq/utils`
are all under active development within the new monorepo. Check the hook page within the documentation 
site to view its development status

<div style={{ display: 'flex', gap: 15, marginTop:'1rem' }}>
<Button href='/docs/contributing' style={{background:'#2248', padding:'.5rem 1rem', borderRadius:'.5rem', textDecoration:'none'}}>Contributing</Button>
<Button href='https://github.com/pratiqdev/hook-docs' style={{background:'#2248', padding:'.5rem 1rem', borderRadius:'.5rem', textDecoration:'none'}}>GitHub Repo</Button>
<Button href='https://github.com/pratiqdev/hook-docs/issues' style={{background:'#2248', padding:'.5rem 1rem', borderRadius:'.5rem', textDecoration:'none'}}>Report Issues</Button>
</div>









## Server Side Rendering

Most hooks are targeted towards front-end development, with a focus on client-side rendering while 
attempting to maintain full compatibility with server-side rendering.
Some hooks have dependancies to global variables that are only available in the browser.
These hooks will have the CSR badge:

Hooks that return **components in CSR** will return *null in SRR*.  
Hooks that return **data in CSR** will return *initial or default values of the hook in SSR*.

<ClientBadge />







## React Hooks and Types

The custom hooks provided here use a combination of the following React hooks, HOC's and types

<RelatedTable heading={false}  />






## Testing and Validating

Tests are performed automatically before releasing and deploying a new version of this package.
The suite is powered by `jest` with `@testing-library` for react lifecycle hooks and `chai` for assertions.
Some tests are performed using simple node scripts where abnormal test behavior is required, like .
The subrepo `@pratiq/test` contains tests for all the provided hooks and utilities

  "dependencies": {
    "@babel/core": "^7.22.9",
    "@babel/preset-env": "^7.22.9",
    "@babel/preset-react": "^7.22.5",
    "@pratiq/config": "workspace:*",
    "@pratiq/hooks": "workspace:*",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react-hooks": "^8.0.1",
    "@types/chai": "^4.3.4",
    "@types/debug": "^4.1.7",
    "@types/mocha": "^10.0.1",
    "babel-jest": "^29.6.1",
    "chai": "^4.3.7",
    "cleanup-after-each": "link:@testing-library/react/cleanup-after-each",
    "cross-env": "^7.0.3",
    "debug": "^4.3.4",
    "eslint": "^7.32.0",
    "jest": "^29.6.1",
    "jest-environment-jsdom": "^29.6.1",
    "jsdom": "^22.1.0",
    "tsup": "^6.6.3",
    "typescript": "^4.9.5"
  }
