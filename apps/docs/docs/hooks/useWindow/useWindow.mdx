---
title: useWindow
description: Get info about the current windows size, rotation scroll depth, mouse position and more
hide_title: true
---
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import { Card, MainText, RelatedTable, HeadlessTable, ClientBadge, StatusBadge } from '@site/src/components'

<MainText>
<h1>useWindow</h1>
Get info about the current windows size, rotation scroll depth, mouse position and more
</MainText>





<Tabs>

<TabItem value='sim' label="Simple">

```tsx live 
function UseWindow() {
  const { width, height, x, y } = useWindow()
  return (
    <ul>
      <li>Screen Width: {width}</li>
      <li>Screen Height: {height}</li>
      <li>Mouse X: {x}</li>
      <li>Mouse Y: {y}</li>
    </ul>
  )
}
``` 

</TabItem>

<TabItem value='adv' label="Advanced">

```tsx live 
function UseWindow() {
  const data = useWindow()
  return <pre>{JSON.stringify(data, null, 2)}</pre>
}
``` 

</TabItem>

</Tabs>